<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="megha.jindal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminCreateAccount.CreateAccountActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminLogin.AdminActivity" />
        <activity android:name=".AdminLogin.SignInAdmin" />
        <activity android:name=".AdminLogin.SignUpAdmin" />
        <activity android:name=".AdminLogin.ProfileActivity" />
        <activity android:name=".AdminLogin.LoginActivity" />

    </application>

</manifest>
